# Check if input data satisfies input conditions

This function throws an error if `x` is not a numeric matrix or a data
frame with all numeric-alike variables, or if any elements of `x` is
`NA`.

## Usage

``` r
checkInputdata(x)
```

## Arguments

- x:

  A matrix or data frame.

## Value

No return value
